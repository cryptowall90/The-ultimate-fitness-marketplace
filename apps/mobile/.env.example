# apps/mobile environment. Names only — never commit real values.
# EXPO_PUBLIC_* variables are bundled into the app: PUBLIC VALUES ONLY.
# The Supabase service-role key and Stripe secret key must NEVER appear here.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_API_BASE_URL=http://localhost:8787
